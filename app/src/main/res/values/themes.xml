<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Appealic" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="windowNoTitle">true</item>   <!-- Đặt giá trị này thành true để ẩn tiêu đề của cửa sổ -->

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- Customize your theme here. -->
<!--        <item name="android:fontFamily">@font/futura_bold</item>-->
<!--        <item name="android:textSize">20sp</item>-->
<!--        <item name="android:layout_gravity">center</item>-->
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <!--    bottom sheet dialog theme-->
    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <!--    bottom sheet style-->
    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
<!--        <item name="android:background">@drawable/custom_bottom_sheet</item>-->
    </style>
    <style name="DialogAnimation">

<!--        <item name="android:windowEnterAnimation">@anim/slide_in_bottom</item>-->
<!--        <item name="android:windowExitAnimation">@anim/slide_out_bottom</item>-->
    </style>
</resources>